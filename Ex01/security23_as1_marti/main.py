
class main:

    def __init__(self):
        self.file = file

    def start():
        file_path = input("Hey! Before we start, please give me a file to either en- or decrypt (enter filepath): ")
        file = open(file_path, 'r')
        input("Would you like to encrypt or decrypt a file?")

    self.start()
        
